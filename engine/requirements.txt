# Backward-compatible shim — installs core dependencies
# For grouped install, use: pip install -r requirements/base.txt
# For everything: pip install -r requirements/all.txt
-r requirements/base.txt
